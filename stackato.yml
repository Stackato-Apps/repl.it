name: repl-it
mem: 512M
framework:
  type: node
hooks:
  post-staging:
    - npm install coffee-script
    - git submodule update --init --recursive
processes:
  web: node $HOME/server.js $VCAP_APP_PORT 
ignores: []
